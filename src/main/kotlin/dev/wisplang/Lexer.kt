package dev.wisplang

object Lexer {
    fun lex(tokens: List<Tokenizer.MatureToken>) {
        var i = 0
        while (i < tokens.size) {
            val token = tokens[i]
            when {

            }
            i++
        }
    }
}